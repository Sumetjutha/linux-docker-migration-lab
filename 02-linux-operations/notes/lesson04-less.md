less คืออะไร ?

ถ้า tail

เอาไว้ดู "ท้ายไฟล์"

less

เอาไว้ "เปิดอ่านไฟล์ขนาดใหญ่"

ปัญหาที่เกิดขึ้น

สมมติพี่มีไฟล์

etl.log

ขนาด

50,000 บรรทัด

ถ้าใช้

cat etl.log

Linux จะพ่นออกมาทั้งหมด

50,000 บรรทัด

หน้าจอไหลยาวจนดูแทบไม่ทัน 😅

less ช่วยยังไง ?

less จะเปิดไฟล์แบบ

อ่านทีละหน้า

คล้าย ๆ

Notepad
VS Code
Text Editor

แต่ทำงานใน Terminal

ตัวอย่าง

เปิดไฟล์

less etl.log

แล้วจะเข้าโหมดอ่านไฟล์

ข้างใน less ทำอะไรได้บ้าง ?

เลื่อนลงทีละบรรทัด

Arrow Down

เลื่อนขึ้น

Arrow Up

เลื่อนทีละหน้า

Space

ย้อนกลับทีละหน้า

b
ออกจาก less

อันนี้สำคัญมาก 😂

กด

q

ย่อมาจาก

quit
ทำไม Data Engineer ต้องรู้ less ?

เพราะบางครั้ง

เราไม่ได้อยากดูแค่ท้ายไฟล์

เราอยากเปิดอ่านทั้งไฟล์

เช่น

ETL Log
SQL Script
Config File
CSV ขนาดใหญ่

ถ้าใช้ cat

ข้อมูลจะไหลเต็มหน้าจอ

แต่ less จะอ่านได้สบายกว่าเยอะ

นึกภาพงานจริง

Business แจ้งว่า

Customer ETL ล้ม

พี่เปิด

tail -n 100 etl.log

แล้วเจอ Error

แต่ยังไม่เข้าใจสาเหตุ

พี่เลยเปิด

less etl.log

แล้วค่อย ๆ เลื่อนดู Context ก่อนหน้า

จำง่าย ๆ
cat
=
พ่นทั้งไฟล์

tail
=
ดูท้ายไฟล์

less
=
เปิดอ่านไฟล์
คำถามก่อนทำ Note

พี่คิดว่า

ถ้าไฟล์ Log มี 200,000 บรรทัด

ระหว่าง

cat etl.log

กับ

less etl.log

พี่จะเลือกใช้อะไร ? และเพราะอะไร ?

ตอบแบบคนทำงานจริงได้เลยครับ 😎

less etl.log ไว้ดูทีละหน้า

ถูกต้องเลยครับพี่ 🎯

ถ้าตอบแบบคนทำงานจริง

less etl.log

เพราะผมต้องการเปิดอ่านไฟล์ขนาดใหญ่แบบค่อย ๆ ดูทีละหน้า ไม่ใช่พ่นออกมาทั้งหน้าจอเหมือน cat

นี่คือ Concept ที่ผมอยากให้ติดหัวเลย

cat
=
พ่นทั้งไฟล์

tail
=
ดูท้ายไฟล์

less
=
เปิดอ่านไฟล์ใหญ่แบบค่อย ๆ อ่าน

อีกมุมนึงที่ Data Engineer ใช้จริง

สมมติ

tail -n 100 etl.log

เจอ

ERROR : Customer Key Missing

แต่ไม่รู้ว่าก่อนหน้าเกิดอะไรขึ้น

ก็จะเปิด

less etl.log

แล้วค่อยเลื่อนย้อนขึ้นไปดู

Load Customer
Validate Customer
Generate Customer Key
ERROR : Customer Key Missing

อ๋อ... เข้าใจละ

อีกเรื่องที่ควรจำ

เปิด less แล้วออกไม่ได้

เป็นโรคประจำตัวมือใหม่ Linux 😂

วิธีออก

q

ย่อมาจาก

quit

สรุป Lesson นี้แบบสั้นที่สุด

cat
=
เททั้งไฟล์

less
=
เปิดอ่านไฟล์

q
=
ออกจาก less